@model IEnumerable<ProyectoVueling.Models.ModulosCursoAmedida>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdCursoAmedidaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdModuloAmedidaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProfesorNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCursoAmedidaNavigation.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdModuloAmedidaNavigation.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProfesorNavigation.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
